CREATE TABLE MASTER (
    FILING_NUM INT PRIMARY KEY NOT NULL,
    LAYOUT_CODE INT NOT NULL,
    STATUS_ID INT,
    CORP_TYPE_ID INT,
    NAME TEXT,
    PERPETUAL_FLAG INT,
    CREATION_DATE INT,
    EXPIRATION_DATE INT,
    INACTIVE_DATE INT,
    FORMATION_DATE INT,
    REPORT_DUE_DATE INT,
    TAX_ID INT,
    DBA_NAME TEXT,
    FOREIGN_FEIN TEXT,
    FOREIGN_STATE TEXT,
    FOREIGN_COUNTRY TEXT,
    FOREIGN_FORMATION_DATE INT,
    EXPIRATION_TYPE TEXT,
    NONPROFIT_SUBTYPE_ID INT,
    BOC_FLAG INT,
    BOC_DATE DATE
);